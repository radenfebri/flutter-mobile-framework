class Titan {
  int _powerPoint;

  Titan(this._powerPoint);

  // Getter untuk mendapatkan nilai _powerPoint.
  int get powerPoint => _powerPoint;

  // Setter untuk mengubah nilai _powerPoint.
  set powerPoint(int value) {
    _powerPoint = value;
  }
}
